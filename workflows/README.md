# Workflows

Store exported n8n workflow definitions here as JSON files. Use descriptive, lowercase, hyphenated names such as `daily-customer-sync.json`.

Before committing an export, review it for embedded credentials, private URLs, or production-specific values. Keep secrets in n8n credentials or environment variables, not in this directory.
