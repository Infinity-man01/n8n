# Deployment

Keep deployment assets here, such as Docker Compose files, infrastructure manifests, reverse-proxy configuration, and environment-variable templates.

Do not commit populated secret files. Provide safe placeholders and document how each environment receives its secrets.
